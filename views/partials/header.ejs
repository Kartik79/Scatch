<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet" />
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="font-['helvetica_now_display']">
    <nav class="w-full flex justify-between px-5 py-3">
    <h3 class="text-xl">Scatch</h3>
    <% if (userType === 'user' && loggedin) { %>
        <div class="flex gap-5">
            <a href="/users/shop">Shop</a>
            <a href="/users/cart">Cart</a>
            <a href="/users/profile">My Account</a>
            <a class="text-red-600" href="/users/logout">Logout</a>
        </div>
    <% } else if (userType === 'owner' && loggedin) { %>
        <div class="flex gap-5">
            <a href="/owners/admin">Dashboard</a>
            <a href="/owners/createproducts">Create</a>
            <a class="text-red-600" href="/owners/logout">Logout</a>
        </div>
    <% } %>
</nav>

